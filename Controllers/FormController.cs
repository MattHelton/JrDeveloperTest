using System;
namespace JrDeveloperTest.Controllers
{
    public class FormController
    {
        public FormController()
        {
            // This controller should take in data from the Form and FormContext models

            // After recieving data, it should return it so that the data is accessible by the views

            // Then create tests to make sure it does these things successfully
        }
    }
}
